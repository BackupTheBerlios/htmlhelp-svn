diff --speed-large-files --minimal -Nru tmp/glibc-2.3.2/configure work/main.d/glibc-2.3.2/configure
--- tmp/glibc-2.3.2/configure	2003-02-26 09:20:48.000000000 +0000
+++ work/main.d/glibc-2.3.2/configure	2003-11-03 12:57:59.000000000 +0000
@@ -1346,12 +1346,6 @@
 ac_configure="$SHELL $ac_aux_dir/configure" # This should be Cygnus configure.
 
 
-if test "`cd $srcdir; /bin/pwd`" = "`/bin/pwd`"; then
-  { { echo "$as_me:$LINENO: error: you must configure in a separate build directory" >&5
-echo "$as_me: error: you must configure in a separate build directory" >&2;}
-   { (exit 1); exit 1; }; }
-fi
-
 # This will get text that should go into config.make.
 config_vars=
 
